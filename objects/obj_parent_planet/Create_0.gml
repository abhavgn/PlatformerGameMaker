// 1. Movement Setup
image_speed = 0.5;
speed = random_range(2, 4); 
direction = random(360);

// 2. Random Scale (Optional)
var _scale = random_range(0.5, 0.7);
image_xscale = _scale;
image_yscale = _scale;