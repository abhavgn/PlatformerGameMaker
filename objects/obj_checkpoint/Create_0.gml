// --- Checkpoint Initialization ---

// Start as the unclaimed (red) flag
sprite_index = spr_checkpoint_unclaimed;

// Set a variable to prevent the player from triggering the logic multiple times
is_active = true;